---
export const prerender = false;

import AdminLayout from "../../layouts/AdminLayout.astro";
import TimelineEditor from "../../components/admin/TimelineEditor.svelte";
import { getAllTimeline } from "../../lib/repositories/timeline";

const timeline = getAllTimeline();
---

<AdminLayout title="Timeline">
	<TimelineEditor client:load items={timeline} />
</AdminLayout>
